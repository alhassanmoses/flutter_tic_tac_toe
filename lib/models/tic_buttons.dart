import 'package:flutter/material.dart';

class TicButtons {
  final int index;
  Widget icon;
  bool isWidgetSet;
  int winVal;
  TicButtons({this.index: -1, this.icon, this.isWidgetSet, this.winVal});
}
